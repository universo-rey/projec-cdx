# PROJEC CDX Root Inventory

Inventario actual de la raiz `C:\Users\enzo1\PROJEC CDX`.

## Resumen

- Total de entradas raiz: 15.
- Archivos raiz: `README.md`, `AGENTS.md`, `MAPA_MAESTRO.md`.
- Carpetas gobernadas: `atomic`, `dataverse`, `docs`, `hitos`, `inventarios`, `operativa`, `outputs`, `packages`, `playbooks`, `tools`, `workbooks`.
- Dependencia excluida como artefacto de trabajo: `node_modules`.

## Lectura

- `operativa` es el plano de control.
- `playbooks` define la ejecucion por deltas.
- `dataverse` es el carril gobernado para evidencia local, metadata-only y gates live.
- `workbooks` contiene libros vivos o de uso operativo.
- `outputs` contiene corridas fechadas y evidencia generada.
- `hitos` versiona cierres y readbacks durables.

## Regla

Este inventario describe `PROJEC CDX`. El inventario `CODEX_ROOT_INVENTORY.*` describe principalmente `.codex` y se conserva como inventario historico/de superficie global.
