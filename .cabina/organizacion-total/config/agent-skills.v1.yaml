schema_version: 1
baseline: SDU_ORG_TOTAL_G1_V3_BASELINE_CANDIDATE_20260623
skills:
  - skill_id: classification_skills
    agent_id: SESHAT
    role: ROUTER_CANONICO
    purpose: clasificar universo y ambiguedad por metadata
    inputs: [metadata, registries, readbacks]
    outputs: [classification_readback]
    allowed_operations: [read_metadata, generate_readback, generate_registry, dry_run_validate]
    forbidden_operations: [delete, move, overwrite, apply_real, live_write, push, pr, secret_exposure]
    required_gate: owner_gate_for_any_mutation
    evidence_output: classification_readback
    rollback: no_mutation_or_git_revert_only_if_owner_authorizes
    validator: git_diff_check_and_staging_empty
    status: active_declarative
  - skill_id: evidence_skills
    agent_id: MAAT
    role: EVIDENCIA_AUDITORIA
    purpose: separar evidencia saneada de evidencia cruda
    inputs: [metadata, registries, readbacks]
    outputs: [evidence_matrix]
    allowed_operations: [read_metadata, generate_readback, generate_registry, dry_run_validate]
    forbidden_operations: [delete, move, overwrite, apply_real, live_write, push, pr, secret_exposure]
    required_gate: owner_gate_for_any_mutation
    evidence_output: evidence_matrix
    rollback: no_mutation_or_git_revert_only_if_owner_authorizes
    validator: git_diff_check_and_staging_empty
    status: active_declarative
  - skill_id: guardrail_skills
    agent_id: ANUBIS
    role: GUARD_GATE
    purpose: bloquear delete move overwrite apply live push pr
    inputs: [metadata, registries, readbacks]
    outputs: [gate_decision]
    allowed_operations: [read_metadata, generate_readback, generate_registry, dry_run_validate]
    forbidden_operations: [delete, move, overwrite, apply_real, live_write, push, pr, secret_exposure]
    required_gate: owner_gate_for_any_mutation
    evidence_output: gate_decision
    rollback: no_mutation_or_git_revert_only_if_owner_authorizes
    validator: git_diff_check_and_staging_empty
    status: active_declarative
  - skill_id: registry_skills
    agent_id: THOT
    role: ARQUITECTO_TECNICO
    purpose: mantener registries declarativos sin agentes productivos
    inputs: [metadata, registries, readbacks]
    outputs: [registry_diff]
    allowed_operations: [read_metadata, generate_readback, generate_registry, dry_run_validate]
    forbidden_operations: [delete, move, overwrite, apply_real, live_write, push, pr, secret_exposure]
    required_gate: owner_gate_for_any_mutation
    evidence_output: registry_diff
    rollback: no_mutation_or_git_revert_only_if_owner_authorizes
    validator: git_diff_check_and_staging_empty
    status: active_declarative
  - skill_id: vscode_operator_skills
    agent_id: THOT
    role: ARQUITECTO_TECNICO
    purpose: operar tasks locales sin configuracion global
    inputs: [metadata, registries, readbacks]
    outputs: [vscode_readback]
    allowed_operations: [read_metadata, generate_readback, generate_registry, dry_run_validate]
    forbidden_operations: [delete, move, overwrite, apply_real, live_write, push, pr, secret_exposure]
    required_gate: owner_gate_for_any_mutation
    evidence_output: vscode_readback
    rollback: no_mutation_or_git_revert_only_if_owner_authorizes
    validator: git_diff_check_and_staging_empty
    status: active_declarative
  - skill_id: git_hygiene_skills
    agent_id: ANUBIS
    role: GUARD_GATE
    purpose: mantener pathspec y staging controlado
    inputs: [metadata, registries, readbacks]
    outputs: [git_scope_report]
    allowed_operations: [read_metadata, generate_readback, generate_registry, dry_run_validate]
    forbidden_operations: [delete, move, overwrite, apply_real, live_write, push, pr, secret_exposure]
    required_gate: owner_gate_for_any_mutation
    evidence_output: git_scope_report
    rollback: no_mutation_or_git_revert_only_if_owner_authorizes
    validator: git_diff_check_and_staging_empty
    status: active_declarative
  - skill_id: manual_review_skills
    agent_id: SESHAT
    role: ROUTER_CANONICO
    purpose: preparar cola de revision manual sin resolver legalmente
    inputs: [metadata, registries, readbacks]
    outputs: [manual_review_queue]
    allowed_operations: [read_metadata, generate_readback, generate_registry, dry_run_validate]
    forbidden_operations: [delete, move, overwrite, apply_real, live_write, push, pr, secret_exposure]
    required_gate: owner_gate_for_any_mutation
    evidence_output: manual_review_queue
    rollback: no_mutation_or_git_revert_only_if_owner_authorizes
    validator: git_diff_check_and_staging_empty
    status: active_declarative
  - skill_id: multiple_triage_skills
    agent_id: HORUS
    role: OBSERVABILIDAD_DRIFT
    purpose: separar multiple legitimo de ruido sin bajar proteccion
    inputs: [metadata, registries, readbacks]
    outputs: [multiple_queue]
    allowed_operations: [read_metadata, generate_readback, generate_registry, dry_run_validate]
    forbidden_operations: [delete, move, overwrite, apply_real, live_write, push, pr, secret_exposure]
    required_gate: owner_gate_for_any_mutation
    evidence_output: multiple_queue
    rollback: no_mutation_or_git_revert_only_if_owner_authorizes
    validator: git_diff_check_and_staging_empty
    status: active_declarative
  - skill_id: baseline_management_skills
    agent_id: MAAT
    role: EVIDENCIA_AUDITORIA
    purpose: custodiar baseline y manifiestos
    inputs: [metadata, registries, readbacks]
    outputs: [baseline_readback]
    allowed_operations: [read_metadata, generate_readback, generate_registry, dry_run_validate]
    forbidden_operations: [delete, move, overwrite, apply_real, live_write, push, pr, secret_exposure]
    required_gate: owner_gate_for_any_mutation
    evidence_output: baseline_readback
    rollback: no_mutation_or_git_revert_only_if_owner_authorizes
    validator: git_diff_check_and_staging_empty
    status: active_declarative
  - skill_id: reporting_skills
    agent_id: MAAT
    role: EVIDENCIA_AUDITORIA
    purpose: producir cierres navegables y auditables
    inputs: [metadata, registries, readbacks]
    outputs: [final_readback]
    allowed_operations: [read_metadata, generate_readback, generate_registry, dry_run_validate]
    forbidden_operations: [delete, move, overwrite, apply_real, live_write, push, pr, secret_exposure]
    required_gate: owner_gate_for_any_mutation
    evidence_output: final_readback
    rollback: no_mutation_or_git_revert_only_if_owner_authorizes
    validator: git_diff_check_and_staging_empty
    status: active_declarative
